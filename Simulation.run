#!/bin/bash  
rm sim.data
rm sim-1.data
rm test.txt

touch sim-1.data
echo "Lambda,Average Throughput, Cpu Utilization" >> sim-1.data

make
for ((i = 1; i <= 30; i++)); do  
   ./main 1 $i 0.06 0.01 >> test.txt
   cat sim.data >> sim-1.data
done 